<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
		x:Class="CryptorApp.MainWindow" xmlns:local="clr-namespace:CryptorApp" xmlns:Behaviors="http://schemas.microsoft.com/xaml/behaviors" xmlns:Specialized="clr-namespace:System.Collections.Specialized;assembly=System.Collections.Specialized"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}, Path=MainViewModel}"
		Title="Cryptor" Height="400" Width="800" MinHeight="240" MinWidth="480" WindowStartupLocation="CenterScreen" FontSize="14" ResizeMode="CanResizeWithGrip">
	<Window.Effect>
		<DropShadowEffect Opacity="0.66" BlurRadius="7" ShadowDepth="2" RenderingBias="Quality" Color="#FF1166DD"/>
	</Window.Effect>
	<Grid Margin="0,12,12,0">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" MinHeight="36"/>
			<RowDefinition Height="16"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto" MinHeight="36"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="12"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="*"/>
			<ColumnDefinition Width="7*"/>
		</Grid.ColumnDefinitions>
		<Label VerticalContentAlignment="Center" FontWeight="Bold" Content="Type"/>
		<ComboBox Grid.Column="1" x:Name="cboConverters" ItemsSource="{Binding Converters, Mode=OneTime}" DisplayMemberPath="Name" VerticalAlignment="Center" FontWeight="Bold" MinHeight="36" SelectedItem="{Binding CurrentCryptor, Mode=TwoWay}" VerticalContentAlignment="Center">
			<Behaviors:Interaction.Triggers>
				<Behaviors:EventTrigger EventName="SelectionChanged">
					<Behaviors:InvokeCommandAction Command="{Binding Select}" />
				</Behaviors:EventTrigger>
			</Behaviors:Interaction.Triggers>
		</ComboBox>
		<ContentControl Grid.ColumnSpan="2" Grid.Row="2" x:Name="ccSettings" Content="{Binding CurrentSettings, Mode=OneWay}"/>
		<Label Grid.Row="3" VerticalContentAlignment="Center" FontWeight="Bold" Content="Input"/>
		<TextBox x:Name="txtInput" Grid.Column="1" Grid.Row="3" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" BorderThickness="1" Text="{Binding Input, Mode=TwoWay}"/>
		<Button x:Name="btnConvert" ToolTip="Convert..." Command="{Binding Convert}" FontFamily="Marlett" FontSize="26" Cursor="Hand" Content="6" Grid.Column="1" Grid.Row="4" HorizontalAlignment="Center" VerticalAlignment="Center" Width="30" Height="30"/>
		<Label Grid.Row="5" VerticalContentAlignment="Center" FontWeight="Bold" Content="Output"/>
		<TextBox x:Name="txtOutput" Grid.Column="1" Grid.Row="5" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" BorderThickness="1" IsReadOnly="True" IsReadOnlyCaretVisible="True" Text="{Binding Output, Mode=TwoWay}"/>
	</Grid>
</Window>